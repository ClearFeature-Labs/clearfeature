"""Registry contract: in-memory models, validation, and a pure builder.

This package defines the registry *contract* and validates it. Reading a YAML
file from disk is intentionally deferred to a later slice (see ``loader.py``).
"""
